{
  "name": "themovie",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run json-server\" \"npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "json-server": "json-server --watch db-movie.json --port 5000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "postcss-cli": "^10.1.0",
    "tailwindcss": "^3.2.7"
  },
  "dependencies": {
    "axios": "^1.3.4",
    "concurrently": "^8.0.1"
  }
}
